{
  "name": "blueearthemedia",
  "private": true,
  "version": "1.1.1",
  "scripts": {
    "build:main": "rm -rf dist && mkdir -p dist && cp index.html services.html about.html contact.html privacy.html terms.html 404.html styles.css script.js favicon.svg robots.txt sitemap.xml dist/",
    "build:publishing": "rm -rf dist && mkdir -p dist && cp publishing/index.html publishing/about.html publishing/contact.html publishing/styles.css dist/",
    "deploy": "wrangler deploy",
    "deploy:publishing": "wrangler deploy --config wrangler.publishing.jsonc",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
