{
  "name": "blueearthemedia",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build:main": "rm -rf dist && mkdir -p dist && cp index.html styles.css script.js dist/",
    "build:publishing": "rm -rf dist && mkdir -p dist && cp publishing/index.html publishing/styles.css dist/",
    "deploy": "wrangler deploy",
    "deploy:publishing": "wrangler deploy --config wrangler.publishing.jsonc",
    "preview": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}